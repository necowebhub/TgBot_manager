# Инструкция по настройке автоматической проверки подписок

## Структура файлов

Добавьте в ваш проект следующие файлы:

```
src/
├── subscription_checker.py  # Модуль проверки и удаления пользователей
├── scheduler.py             # Планировщик ежедневных проверок и ежечасной синхронизации
├── db.py                    # Обновленный файл с новым методом
├── main.py                  # Обновленный файл запуска
└── handlers/
    ├── user/
    │   └── message.py       # Обработчик сообщений пользователей
    ├── channel/
    │   └── manager.py       # Обработчик канала (сейчас пустой)
    └── admin/
        └── commands.py      # Новый: команды администратора
```

## Настройка .env файла

Добавьте в ваш `.env` файл следующие параметры:

```env
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=@your_channel_name  # или -100123456789 (числовой ID)
DONATION_ALERTS_TOKEN=your_donation_alerts_token

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Время ежедневной проверки (по умолчанию 12:00)
CHECK_HOUR=12
CHECK_MINUTE=0
```

## Как получить ID пользователя (для ADMIN_IDS)

1. **Через бота @userinfobot**:
   - Откройте бота
   - Нажмите /start
   - Бот покажет ваш ID

2. **Через бота @getmyid_bot**:
   - Откройте бота
   - Нажмите /start
   - Бот покажет ваш ID

3. **Добавьте несколько ID через запятую**:
   ```env
   ADMIN_IDS=123456789,987654321,555555555
   ```

## Как получить ID канала

1. **Через бота @username_to_id_bot**:
   - Добавьте бота в канал
   - Перешлите любое сообщение из канала боту
   - Он вернет ID канала

2. **Через @getmyid_bot**:
   - Добавьте бота в канал как администратора
   - Напишите любое сообщение в канал
   - Бот покажет ID

3. **Если у канала есть публичное имя**: используйте формат `@channel_name`

## Настройка прав бота в канале

Бот должен быть добавлен в канал как **администратор** со следующими правами:

- ✅ Банить пользователей (Ban users)
- ✅ Добавлять администраторов (Add administrators) - опционально

**Важно**: Без прав администратора бот не сможет удалять пользователей!

### Как добавить бота администратором:

1. Откройте канал → Настройки → Администраторы
2. Нажмите "Добавить администратора"
3. Найдите вашего бота
4. Установите необходимые права
5. Сохраните

## Формат сообщений в донатах

Для корректной работы, в сообщении доната должен быть указан username пользователя. Например:

- `Донат от @username`
- `Спасибо за стрим! username`
- `@username - продлеваю подписку`

Бот будет искать `@username` в тексте сообщения.

## Запуск

```bash
python src/main.py
```

## Функционал

### Автоматическая синхронизация донатов

- Бот автоматически **каждый час** запрашивает новые донаты через DonationAlerts API
- Обновляет базу данных с новыми донатами
- Продлевает подписки пользователей согласно сумме донатов
- Логирует статистику обработки

### Автоматическая проверка подписок

- Бот автоматически проверяет подписки **раз в день** в указанное время (по умолчанию 12:00 по МСК)
- Пользователи с истекшей подпиской автоматически удаляются из канала
- Логи выводятся в консоль

### Команды администратора (в личных сообщениях)

Все команды администратора работают **только в личных сообщениях с ботом**, не засоряя канал:

| Команда | Описание |
|---------|----------|
| `/admin` | Показать меню администратора |
| `/sync` | Синхронизировать донаты за последний час |
| `/check` | Проверить подписки и удалить просроченные |
| `/stats` | Получить статистику по базе данных |
| `/user [username]` | Получить информацию о конкретном пользователе |

**Примеры использования:**
```
/admin
/sync
/check
/stats
/user johndoe
/user @johndoe
```

## Логика работы

### Синхронизация донатов (каждый час):
1. Бот запрашивает донаты за последний час через DonationAlerts API
2. Для каждого доната извлекается username, сумма и дата
3. Если пользователь уже есть в базе - обновляется сумма и продлевается подписка
4. Если пользователь новый - создается новая запись
5. Каждые 200 рублей = +1 месяц подписки

### Проверка подписок (раз в день):
1. Бот получает из базы данных список пользователей, у которых поле `sub` (дата окончания подписки) меньше текущей даты
2. Извлекает username из поля `message`
3. Находит пользователя в канале по username
4. Банит пользователя (удаляет из канала)
5. Опционально: можно сразу разбанить, чтобы он мог вернуться при новой подписке

### Команды администратора:
- Работают только для пользователей, указанных в `ADMIN_IDS`
- Выполняются в личных сообщениях с ботом
- Возвращают результаты в личные сообщения
- Не засоряют канал

## Настройка поведения

### Изменить время проверки подписок

В `.env` файле:
```env
CHECK_HOUR=15    # Проверка в 15:00
CHECK_MINUTE=30  # Проверка в 15:30
```

### Изменить частоту синхронизации донатов

По умолчанию синхронизация происходит каждый час. Чтобы изменить это, отредактируйте в `scheduler.py`:

```python
await asyncio.sleep(3600)  # 3600 секунд = 1 час
```

Например, для синхронизации каждые 30 минут:
```python
await asyncio.sleep(1800)  # 1800 секунд = 30 минут
```

### Разбанить пользователя после удаления

В файле `subscription_checker.py`, раскомментируйте строку:

```python
# await bot.unban_chat_member(channel_id, user_id_to_ban)
```

Это позволит пользователю вернуться по новой ссылке-приглашению после продления подписки.

## Тестирование

1. Запустите бота
2. Откройте личные сообщения с ботом
3. Отправьте `/admin` для проверки доступа
4. Попробуйте команды:
   - `/stats` - посмотреть статистику
   - `/sync` - синхронизировать донаты
   - `/check` - проверить подписки
   - `/user username` - информация о пользователе

## Возможные проблемы

### Команды администратора

**"⛔ У вас нет прав администратора"**
- Проверьте, что ваш ID добавлен в `ADMIN_IDS` в `.env`
- Получите свой ID через @userinfobot
- Убедитесь, что ID указаны через запятую без пробелов
- Перезапустите бота после изменения `.env`

### Синхронизация донатов

**"Ошибка при запросе к API"**
- Проверьте правильность токена DonationAlerts
- Убедитесь, что токен не истек
- Проверьте права доступа токена

**"Донаты не найдены"**
- Возможно, за последний час не было донатов
- Проверьте корректность дат в запросе

### Проверка подписок

**"Not enough rights"**
- Убедитесь, что бот является администратором канала
- Проверьте, что у бота есть права на бан пользователей

**"User not found"**
- Пользователь не является участником канала
- Username в базе данных указан неверно
- Пользователь изменил username

**Username не извлекается**
- Проверьте формат сообщений в базе данных
- Адаптируйте функцию `extract_username_from_message()` под ваш формат

## Мониторинг

Все действия бота логируются в консоль:

### Синхронизация донатов:
```
[2025-01-21 15:00:00] Начало синхронизации донатов...
Загрузка страницы 1...
✓ Синхронизация завершена:
  - Всего обработано: 5
  - Добавлено новых: 3
  - Обновлено: 2
  - Ошибок: 0
Следующая синхронизация через 1 час...
```

### Проверка подписок:
```
[2025-01-21 12:00:00] Начало проверки подписок...
Найдено 5 записей с истекшей подпиской
✓ Пользователь @user1 удален из канала (подписка до 2025-01-20)
✓ Пользователь @user2 удален из канала (подписка до 2025-01-19)
...
=== Результаты проверки ===
Проверено записей: 5
Удалено пользователей: 3
Ошибок: 2
```

### Команды администратора (в личных сообщениях):

**Статистика (/stats):**
```
📊 Статистика базы данных

👥 Всего пользователей: 150
💰 Общая сумма донатов: 45000.00 руб.
✅ Активных подписок: 120
❌ Истекших подписок: 30
📈 Средний донат: 300.00 руб.
```

**Информация о пользователе (/user johndoe):**
```
👤 Информация о пользователе @johndoe

💰 Сумма донатов: 1200 руб.
📅 Последний донат: 2025-01-15
🎫 Подписка до: 2025-07-15
📊 Статус: ✅ Активна (осталось 175 дней)
```

## Рекомендации

1. **Синхронизация донатов**:
   - Убедитесь, что токен DonationAlerts имеет необходимые права
   - При первом запуске можно уменьшить интервал для тестирования
   - Следите за логами на предмет ошибок API

2. **Проверка подписок**:
   - Настройте время проверки на время наименьшей активности канала
   - Протестируйте на тестовом канале перед использованием на основном
   - Регулярно проверяйте логи на предмет ошибок

3. **Администрирование**:
   - Сохраните список `ADMIN_IDS` в безопасном месте
   - Не делитесь ID администраторов с посторонними
   - Регулярно используйте `/stats` для мониторинга
   - Используйте `/user` для проверки конкретных пользователей при обращениях

4. **Общие рекомендации**:
   - Сохраняйте резервные копии базы данных
   - Информируйте пользователей о необходимости указывать username в донатах
   - Мониторьте работу бота через логи
   - При необходимости настройте систему оповещений об ошибках

## Преимущества новой системы

✅ **Чистота канала** - все административные команды в личных сообщениях  
✅ **Безопасность** - только указанные ID имеют доступ к командам  
✅ **Удобство** - вся статистика и управление в одном месте  
✅ **Гибкость** - легко добавлять и удалять администраторов через .env  
✅ **Информативность** - детальная статистика по каждому пользователю

## Как получить ID канала

1. **Через бота @username_to_id_bot**:
   - Добавьте бота в канал
   - Перешлите любое сообщение из канала боту
   - Он вернет ID канала

2. **Через @getmyid_bot**:
   - Добавьте бота в канал как администратора
   - Напишите любое сообщение в канал
   - Бот покажет ID

3. **Если у канала есть публичное имя**: используйте формат `@channel_name`

## Настройка прав бота в канале

Бот должен быть добавлен в канал как **администратор** со следующими правами:

- ✅ Банить пользователей (Ban users)
- ✅ Добавлять администраторов (Add administrators) - опционально

**Важно**: Без прав администратора бот не сможет удалять пользователей!

### Как добавить бота администратором:

1. Откройте канал → Настройки → Администраторы
2. Нажмите "Добавить администратора"
3. Найдите вашего бота
4. Установите необходимые права
5. Сохраните

## Формат сообщений в донатах

Для корректной работы, в сообщении доната должен быть указан username пользователя. Например:

- `Донат от @username`
- `Спасибо за стрим! username`
- `@username - продлеваю подписку`

Бот будет искать `@username` в тексте сообщения.

## Запуск

```bash
python src/main.py
```

## Функционал

### Автоматическая синхронизация донатов

- Бот автоматически **каждый час** запрашивает новые донаты через DonationAlerts API
- Обновляет базу данных с новыми донатами
- Продлевает подписки пользователей согласно сумме донатов
- Логирует статистику обработки

### Автоматическая проверка подписок

- Бот автоматически проверяет подписки **раз в день** в указанное время (по умолчанию 12:00 по МСК)
- Пользователи с истекшей подпиской автоматически удаляются из канала
- Логи выводятся в консоль

### Ручная синхронизация донатов

Администраторы канала могут запустить синхронизацию вручную командой в канале:

```
/sync_donations
```

Бот синхронизирует донаты за последний час и отчитается о результатах.

### Ручная проверка подписок

Администраторы канала могут запустить проверку вручную командой в канале:

```
/check_subscriptions
```

Бот выполнит проверку немедленно и отчитается о результатах.

## Логика работы

### Синхронизация донатов (каждый час):
1. Бот запрашивает донаты за последний час через DonationAlerts API
2. Для каждого доната извлекается username, сумма и дата
3. Если пользователь уже есть в базе - обновляется сумма и продлевается подписка
4. Если пользователь новый - создается новая запись
5. Каждые 200 рублей = +1 месяц подписки

### Проверка подписок (раз в день):
1. Бот получает из базы данных список пользователей, у которых поле `sub` (дата окончания подписки) меньше текущей даты
2. Извлекает username из поля `message`
3. Находит пользователя в канале по username
4. Банит пользователя (удаляет из канала)
5. Опционально: можно сразу разбанить, чтобы он мог вернуться при новой подписке

## Настройка поведения

### Изменить время проверки подписок

В `.env` файле:
```env
CHECK_HOUR=15    # Проверка в 15:00
CHECK_MINUTE=30  # Проверка в 15:30
```

### Изменить частоту синхронизации донатов

По умолчанию синхронизация происходит каждый час. Чтобы изменить это, отредактируйте в `scheduler.py`:

```python
await asyncio.sleep(3600)  # 3600 секунд = 1 час
```

Например, для синхронизации каждые 30 минут:
```python
await asyncio.sleep(1800)  # 1800 секунд = 30 минут
```

### Разбанить пользователя после удаления

В файле `subscription_checker.py`, раскомментируйте строку:

```python
# await bot.unban_chat_member(channel_id, user_id_to_ban)
```

Это позволит пользователю вернуться по новой ссылке-приглашению после продления подписки.

## Тестирование

1. Запустите бота
2. В канале выполните команду `/sync_donations` для проверки синхронизации
3. Выполните команду `/check_subscriptions` для проверки удаления пользователей
4. Проверьте логи в консоли
5. Убедитесь, что все работает корректно

## Возможные проблемы

### Синхронизация донатов

**"Ошибка при запросе к API"**
- Проверьте правильность токена DonationAlerts
- Убедитесь, что токен не истек
- Проверьте права доступа токена

**"Донаты не найдены"**
- Возможно, за последний час не было донатов
- Проверьте корректность дат в запросе

### Проверка подписок

**"Not enough rights"**
- Убедитесь, что бот является администратором канала
- Проверьте, что у бота есть права на бан пользователей

**"User not found"**
- Пользователь не является участником канала
- Username в базе данных указан неверно
- Пользователь изменил username

**Username не извлекается**
- Проверьте формат сообщений в базе данных
- Адаптируйте функцию `extract_username_from_message()` под ваш формат

## Мониторинг

Все действия бота логируются в консоль:

### Синхронизация донатов:
```
[2025-01-21 15:00:00] Начало синхронизации донатов...
Загрузка страницы 1...
✓ Синхронизация завершена:
  - Всего обработано: 5
  - Добавлено новых: 3
  - Обновлено: 2
  - Ошибок: 0
Следующая синхронизация через 1 час...
```

### Проверка подписок:
```
[2025-01-21 12:00:00] Начало проверки подписок...
Найдено 5 записей с истекшей подпиской
✓ Пользователь @user1 удален из канала (подписка до 2025-01-20)
✓ Пользователь @user2 удален из канала (подписка до 2025-01-19)
...
=== Результаты проверки ===
Проверено записей: 5
Удалено пользователей: 3
Ошибок: 2
```

## Рекомендации

1. **Синхронизация донатов**:
   - Убедитесь, что токен DonationAlerts имеет необходимые права
   - При первом запуске можно уменьшить интервал для тестирования
   - Следите за логами на предмет ошибок API

2. **Проверка подписок**:
   - Настройте время проверки на время наименьшей активности канала
   - Протестируйте на тестовом канале перед использованием на основном
   - Регулярно проверяйте логи на предмет ошибок

3. **Общие рекомендации**:
   - Сохраняйте резервные копии базы данных
   - Информируйте пользователей о необходимости указывать username в донатах
   - Мониторьте работу бота через логи
   - При необходимости настройте систему оповещений об ошибках

## Команды для администраторов

| Команда | Описание |
|---------|----------|
| `/sync_donations` | Синхронизировать донаты за последний час |
| `/check_subscriptions` | Проверить подписки и удалить просроченные |

Обе команды доступны только администраторам канала и выполняются в самом канале.