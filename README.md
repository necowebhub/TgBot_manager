# TgBot_manager - Полное руководство

Телеграм-бот для автоматизации управления платной подпиской на канал через систему донатов DonationAlerts.

---

## 📋 Содержание

1. [Обзор системы](##обзор-системы)
2. [Требования](##требования)
3. [Установка](##установка)
4. [Настройка](##настройка)
5. [Функционал](##функционал)
6. [Архитектура](##архитектура)
7. [Логирование](##логирование)
8. [Устранение неполадок](##устранение-неполадок)
9. [Безопасность](##безопасность)
10. [FAQ](##faq)

---

## 🎯 Обзор системы

### Что делает бот?

Бот автоматизирует управление платной подпиской на Telegram-канал:

- **Синхронизирует донаты** из DonationAlerts каждый час
- **Вычисляет срок подписки**: 200₽ = 1 месяц подписки
- **Выдает персональные ссылки-приглашения** пользователям с активной подпиской
- **Автоматически удаляет** пользователей с истекшей подпиской
- **Предоставляет админ-панель** для управления и статистики

### Преимущества

✅ **Полная автоматизация** - никаких ручных операций  
✅ **Прозрачность** - пользователи видят свой статус в реальном времени  
✅ **Гибкость** - донаты накапливаются и продлевают подписку  
✅ **Безопасность** - персональные одноразовые ссылки  
✅ **Мониторинг** - детальное логирование всех операций

---

## 💻 Требования

### Системные требования

- Python 3.8 или выше
- SQLite3 (входит в стандартную библиотеку Python)
- 50+ МБ свободного места на диске
- Стабильное интернет-соединение

### Библиотеки Python

```bash
aiogram==3.24.0
python-decouple==3.8
requests (установится автоматически как зависимость)
```

### Внешние сервисы

1. **Telegram Bot** - создайте бота через [@BotFather](https://t.me/BotFather)
2. **DonationAlerts аккаунт** - для получения донатов
3. **Telegram Channel** - канал, доступом к которому управляет бот

---

## 📦 Установка

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd TgBot_manager
```

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Создание структуры проекта

Убедитесь, что структура выглядит так:

```
TgBot_manager/
├── src/
│   ├── main.py
│   ├── api.py
│   ├── db.py
│   ├── scheduler.py
│   ├── subscription_checker.py
│   ├── logger_config.py
│   ├── filters/
│   │   ├── __init__.py
│   │   └── chat_type.py
│   ├── handlers/
│   │   ├── user/
│   │   │   ├── __init__.py
│   │   │   └── message.py
│   │   └── admin/
│   │       ├── __init__.py
│   │       └── commands.py
│   └── keyboards/
│       └── user.py
├── logs/              # создается автоматически
├── .env               # нужно создать
├── requirements.txt
└── README.md
```

---

## ⚙️ Настройка

### 1. Создание Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **токен бота**

### 2. Получение токена DonationAlerts

1. Перейдите на [DonationAlerts API](https://www.donationalerts.com/application/clients)
2. Создайте новое приложение
3. Получите **Access Token** с правами на чтение донатов
4. Сохраните токен

### 3. Настройка канала

#### Получение ID канала

**Способ 1: Через @username_to_id_bot**
```
1. Добавьте бота в канал
2. Перешлите любое сообщение из канала боту
3. Бот вернет ID канала (формат: -100XXXXXXXXX)
```

**Способ 2: Через @getmyid_bot**
```
1. Добавьте бота в канал как администратора
2. Напишите любое сообщение в канал
3. Бот покажет ID
```

**Способ 3: Публичный канал**
```
Если у канала есть @username, используйте: @channel_name
```

#### Добавление бота администратором канала

**КРИТИЧЕСКИ ВАЖНО**: Бот ДОЛЖЕН быть администратором канала!

1. Откройте канал → **Управление каналом**
2. Перейдите в **Администраторы**
3. Нажмите **Добавить администратора**
4. Найдите вашего бота по username
5. Включите права:
   - ✅ **Банить пользователей** (обязательно)
   - ✅ **Добавлять администраторов** (опционально)
6. Сохраните изменения

### 4. Получение ID администраторов

Для доступа к админ-панели нужны Telegram ID администраторов:

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите `/start`
3. Бот покажет ваш ID (например: `123456789`)
4. Повторите для всех администраторов

### 5. Создание .env файла

Создайте файл `.env` в корне проекта:

```env
# Токен бота (обязательно)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ID канала (обязательно)
# Формат: -100XXXXXXXXX или @channel_name
CHANNEL_ID=-1001234567890

# Токен DonationAlerts (обязательно)
ACCESS_TOKEN=your_donation_alerts_access_token_here

# ID администраторов через запятую (обязательно)
ADMIN_IDS=123456789,987654321,555555555

# Время ежедневной проверки подписок (опционально)
# По умолчанию: 12:00 по серверному времени
CHECK_HOUR=12
CHECK_MINUTE=0
```

#### Параметры .env в деталях

| Параметр | Обязательный | Описание | Пример |
|----------|--------------|----------|---------|
| `BOT_TOKEN` | ✅ Да | Токен от @BotFather | `1234567890:ABC...` |
| `CHANNEL_ID` | ✅ Да | ID канала или @username | `-1001234567890` |
| `ACCESS_TOKEN` | ✅ Да | Токен DonationAlerts | `eyJ0eXAiOiJKV1...` |
| `ADMIN_IDS` | ✅ Да | ID админов через запятую | `123,456,789` |
| `CHECK_HOUR` | ❌ Нет | Час проверки (0-23) | `12` |
| `CHECK_MINUTE` | ❌ Нет | Минута проверки (0-59) | `0` |

---

## 🚀 Запуск

### Запуск бота

```bash
cd src
python main.py
```

### Проверка успешного запуска

При успешном запуске вы увидите:

```
2025-01-23 12:00:00 - __main__ - INFO - Бот запущен: BotName (@bot_username, ID: 1234567890)
2025-01-23 12:00:00 - __main__ - INFO - Настройки: CHANNEL_ID=-1001234567890, CHECK_TIME=12:00:00
2025-01-23 12:00:00 - __main__ - INFO - Фоновые задачи запущены, начало polling...
```

### Запуск в фоне (Linux/macOS)

```bash
nohup python src/main.py > bot.log 2>&1 &
```

### Запуск через systemd (рекомендуется для production)

Создайте файл `/etc/systemd/system/tgbot.service`:

```ini
[Unit]
Description=TgBot Manager
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/TgBot_manager
ExecStart=/usr/bin/python3 /path/to/TgBot_manager/src/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl daemon-reload
sudo systemctl enable tgbot
sudo systemctl start tgbot
sudo systemctl status tgbot
```

---

## 🎮 Функционал

### Для пользователей

#### 1. Команда /start

**Назначение**: Приветствие и инструкции для пользователя

**Что делает**:
- Проверяет наличие username у пользователя
- Валидирует username (5-32 символа, только буквы, цифры, подчеркивание)
- Показывает клавиатуру с кнопками: **Приватка**, **Я**, **Донат**
- Объясняет функционал бота

**Важно**: Пользователь БЕЗ username не сможет пользоваться ботом!

#### 2. Кнопка "Приватка"

**Назначение**: Получение персональной ссылки-приглашения в канал

**Алгоритм работы**:

1. Проверяет наличие username у пользователя
2. Ищет донаты пользователя в базе данных по username
3. Проверяет срок действия подписки
4. Если подписка активна:
   - Создает одноразовую ссылку-приглашение
   - Отправляет ссылку пользователю
5. Если подписка истекла:
   - Сообщает об истечении
   - Предлагает продлить через кнопку "Донат"

**Особенности ссылок**:
- Одноразовые (работают только для одного входа)
- Персональные (содержат username в названии)
- Автоматически деактивируются после использования

#### 3. Кнопка "Я"

**Назначение**: Проверка статуса подписки и суммы донатов

**Что показывает**:
- Username пользователя
- Общая сумма всех донатов
- Дата последнего доната
- Дата окончания подписки
- Статус: 🟢 Активна (осталось X дней) / 🔴 Истекла

**Пример вывода**:
```
🟢 Статус подписки

Username: @johndoe
Сумма донатов: 600 руб.
Дата последнего доната: 2025-01-15
Подписка действует до: 2025-04-15
Статус: Активна (осталось 82 дня)
```

#### 4. Кнопка "Донат"

**Назначение**: Информация об оплате и ссылка на DonationAlerts

**Что показывает**:
- Напоминание указать username в сообщении доната
- Тарифы (200₽ = 1 месяц)
- Кнопку для перехода на страницу доната
- Информацию о времени обработки (до 1 часа)

**ВАЖНО для пользователей**: 
В сообщении доната ОБЯЗАТЕЛЬНО указать свой @username, иначе система не сможет идентифицировать донат!

### Для администраторов

Все админ-команды работают **только в личных сообщениях с ботом**.

#### 1. Команда /start (для админов)

Показывает админ-панель с доступными командами.

#### 2. Команда /stats

**Назначение**: Получение статистики по базе данных

**Что показывает**:
```
📊 Статистика базы данных

👥 Всего пользователей: 150
💰 Общая сумма донатов: 45000.00 руб.
✅ Активных подписок: 120
❌ Истекших подписок: 30
📈 Средний донат: 300.00 руб.
```

**Когда использовать**:
- Для мониторинга общей активности
- Для анализа популярности канала
- Для финансовой отчетности

#### 3. Команда /sync

**Назначение**: Принудительная синхронизация донатов

**Что делает**:
1. Запрашивает донаты за последний час через DonationAlerts API
2. Обрабатывает каждый донат
3. Добавляет новых пользователей или обновляет существующих
4. Пересчитывает сроки подписки

**Результат**:
```
✅ Синхронизация завершена

Всего обработано: 5
Добавлено новых: 3
Обновлено: 2
Ошибок: 0
```

**Когда использовать**:
- При обращении пользователя о недоступности канала
- Для проверки корректности автоматической синхронизации
- После массовых донатов (например, после стрима)

#### 4. Команда /check

**Назначение**: Принудительная проверка и удаление пользователей с истекшей подпиской

**Что делает**:
1. Находит всех пользователей с истекшей подпиской
2. Извлекает username из сообщения доната
3. Банит пользователей в канале (удаляет из участников)

**Когда использовать**:
- Перед важным мероприятием в канале
- При необходимости срочной очистки канала
- Для проверки работы автоматической системы

**Примечание**: Обычно не требуется, так как проверка происходит автоматически ежедневно.

#### 5. Команда /user [username]

**Назначение**: Детальная информация о конкретном пользователе

**Синтаксис**:
```
/user johndoe
/user @johndoe  (@ необязателен)
```

**Что показывает**:
```
👤 Информация о пользователе @johndoe

💰 Сумма донатов: 1200 руб.
📅 Последний донат: 2025-01-15
🎫 Подписка до: 2025-07-15
📊 Статус: ✅ Активна (осталось 175 дней)
```

**Когда использовать**:
- При обращении пользователя в поддержку
- Для проверки статуса конкретного донатера
- Для разрешения спорных ситуаций

---

## 🤖 Автоматические процессы

### 1. Ежечасная синхронизация донатов

**Частота**: Каждый час  
**Время выполнения**: ~30-60 секунд (зависит от количества донатов)

**Алгоритм**:

1. **Запрос донатов** за последний час через DonationAlerts API
2. **Обработка каждого доната**:
   - Извлечение username из сообщения
   - Проверка существования в базе
   - Если новый:
     - Создание записи
     - Расчет даты подписки: текущая дата + (сумма ÷ 200) месяцев
   - Если существующий:
     - Добавление суммы к текущей
     - Продление подписки: текущая дата окончания + (новая сумма ÷ 200) месяцев

**Пример расчета подписки**:

```python
# Новый пользователь
Донат: 600₽
Подписка: сегодня + 3 месяца = 23.04.2026

# Существующий пользователь
Текущая подписка до: 15.02.2026
Новый донат: 400₽
Новая подписка: 15.02.2026 + 2 месяца = 15.04.2026
```

**Логи синхронизации**:
```
2025-01-23 15:00:00 - Начало синхронизации донатов...
2025-01-23 15:00:05 - Загрузка страницы 1...
2025-01-23 15:00:10 - Всего загружено донатов: 5
2025-01-23 15:00:15 - Добавлен новый донат ID 42: @johndoe - продление подписки (сумма: 400)
2025-01-23 15:00:20 - Обновлен донат ID 38: @alice... (сумма: 600 -> 1000)
2025-01-23 15:00:25 - Синхронизация завершена - обработано: 5, добавлено: 3, обновлено: 2, ошибок: 0
```

### 2. Ежедневная проверка подписок

**Частота**: Один раз в день  
**Время**: Настраивается через `CHECK_HOUR` и `CHECK_MINUTE` (по умолчанию 12:00)  
**Время выполнения**: ~5-10 секунд (зависит от количества истекших подписок)

**Алгоритм**:

1. **Получение списка** пользователей с истекшей подпиской (sub < текущая дата)
2. **Для каждого пользователя**:
   - Извлечение username из сообщения доната
   - Поиск пользователя в канале по username
   - Бан пользователя (удаление из участников)
   - Опционально: разбан (чтобы мог вернуться по новой ссылке)

**Извлечение username**:

Система пытается найти username в сообщении доната различными способами:

1. Поиск @mention: `@johndoe`
2. Поиск по ключевым словам: `username: johndoe`, `ник: johndoe`
3. Поиск валидного username среди слов (5-32 символа, буквы/цифры/подчеркивание)

**Логи проверки**:
```
2025-01-23 12:00:00 - Начало проверки истекших подписок...
2025-01-23 12:00:01 - Найдено 5 записей с истекшей подпиской
2025-01-23 12:00:03 - ✓ Пользователь @user1 удален из канала (подписка до 2025-01-20)
2025-01-23 12:00:05 - ✓ Пользователь @user2 удален из канала (подписка до 2025-01-19)
2025-01-23 12:00:06 - ⚠ Пользователь @user3 не найден в канале
2025-01-23 12:00:10 - Проверка завершена - проверено: 5, удалено: 3, ошибок: 2
```

---

## 🏗️ Архитектура

### Структура базы данных

**Таблица: donations**

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Первичный ключ (автоинкремент) |
| `message` | TEXT | Сообщение доната (UNIQUE, содержит username) |
| `amount` | REAL | Сумма всех донатов пользователя |
| `last_date` | TEXT | Дата последнего доната (ISO 8601) |
| `sub` | TIMESTAMP | Дата окончания подписки (ISO 8601) |
| `created_at` | TIMESTAMP | Дата создания записи |
| `updated_at` | TIMESTAMP | Дата последнего обновления |

**Индексы**:
- `UNIQUE` на `message` - для быстрого поиска и предотвращения дублей

**Примеры записей**:
```sql
id: 1
message: "@johndoe - спасибо за контент!"
amount: 600.0
last_date: "2025-01-15T14:30:00+00:00"
sub: "2025-04-15T14:30:00+00:00"
created_at: "2025-01-15T14:30:00"
updated_at: "2025-01-15T14:30:00"
```

### Модули системы

#### 1. main.py
**Назначение**: Точка входа в приложение

**Функции**:
- Инициализация бота и диспетчера
- Подключение роутеров (user, admin)
- Запуск фоновых задач (синхронизация, проверка)
- Обработка ошибок и graceful shutdown

#### 2. api.py
**Назначение**: Работа с DonationAlerts API

**Класс: DonationAlertsAPI**

Методы:
- `get_donations(page)` - получение донатов (одна страница)
- `get_all_donations_in_range(start_date, end_date)` - получение всех донатов за период
- `format_donation(donation)` - форматирование данных доната
- `export_to_json(donations, filename)` - экспорт в JSON

Особенности:
- Автоматические retry при ошибках (3 попытки)
- Обработка rate limits (экспоненциальная задержка)
- Валидация токена и обработка 401/403 ошибок

#### 3. db.py
**Назначение**: Работа с базой данных SQLite

**Класс: DonationDB**

Основные методы:
- `save_donation(message, amount, last_date)` - сохранение/обновление доната
- `save_donations_batch(donations)` - пакетное сохранение
- `get_all_donations()` - получение всех донатов
- `get_user_donations(username)` - поиск донатов пользователя
- `get_expired_subscriptions()` - получение истекших подписок
- `_calculate_sub_date(base_date, amount)` - расчет даты подписки

Функции:
- `process_donations(start_date, end_date, ACCESS_TOKEN)` - обработка донатов за период
- `user_donations(username)` - получение донатов пользователя
- `get_expired_users()` - получение пользователей с истекшей подпиской

#### 4. scheduler.py
**Назначение**: Планирование автоматических задач

Функции:
- `schedule_hourly_donations_sync(access_token)` - ежечасная синхронизация
- `schedule_daily_check(bot, channel_id, check_time)` - ежедневная проверка
- `run_immediate_sync(access_token)` - немедленная синхронизация
- `run_immediate_check(bot, channel_id)` - немедленная проверка

#### 5. subscription_checker.py
**Назначение**: Проверка и удаление пользователей с истекшей подпиской

Функции:
- `extract_username_from_message(message_text)` - извлечение username из сообщения
- `check_and_remove_expired_subscriptions(bot, channel_id)` - проверка и удаление

#### 6. handlers/user/message.py
**Назначение**: Обработка сообщений пользователей

Хендлеры:
- `/start` - приветствие и инструкции
- `Приватка` - получение ссылки-приглашения
- `Я` - проверка статуса подписки
- `Донат` - информация об оплате

#### 7. handlers/admin/commands.py
**Назначение**: Административные команды

Хендлеры:
- `/start` - админ-панель
- `/stats` - статистика БД
- `/sync` - синхронизация донатов
- `/check` - проверка подписок
- `/user [username]` - информация о пользователе

Функция:
- `is_admin(user_id)` - проверка прав администратора

---

## 📊 Логирование

### Конфигурация логирования

**Файл**: `logger_config.py`

**Уровни логирования**:
- **DEBUG** - детальная отладочная информация
- **INFO** - общая информация о работе
- **WARNING** - предупреждения (не критичные)
- **ERROR** - ошибки выполнения
- **CRITICAL** - критические ошибки

### Файлы логов

Логи сохраняются в директорию `logs/`:

1. **`bot_YYYY-MM-DD.log`** - все события (DEBUG+)
   - Запуск/остановка бота
   - Действия пользователей
   - Синхронизация и проверки
   - Ошибки и предупреждения

2. **`errors_YYYY-MM-DD.log`** - только ошибки (ERROR+)
   - Ошибки API
   - Ошибки БД
   - Ошибки Telegram API
   - Неожиданные исключения

### Примеры логов

**Запуск бота**:
```
2025-01-23 12:00:00 - __main__ - INFO - Бот запущен: MyBot (@my_bot, ID: 1234567890)
2025-01-23 12:00:00 - __main__ - INFO - Настройки: CHANNEL_ID=-1001234567890, CHECK_TIME=12:00:00
2025-01-23 12:00:00 - scheduler - INFO - Планировщик синхронизации донатов запущен
2025-01-23 12:00:00 - scheduler - INFO - Планировщик проверки подписок запущен
```

**Действия пользователя**:
```
2025-01-23 14:23:15 - handlers.user.message - INFO - Пользователь 123456789 (@johndoe) запустил бота
2025-01-23 14:23:45 - handlers.user.message - INFO - Пользователь 123456789 (@johndoe) запросил ссылку-приглашение
2025-01-23 14:23:46 - handlers.user.message - INFO - Создана ссылка-приглашение для @johndoe
```

**Синхронизация донатов**:
```
2025-01-23 15:00:00 - scheduler - INFO - Начало синхронизации донатов...
2025-01-23 15:00:05 - api - INFO - DonationAlertsAPI инициализирован
2025-01-23 15:00:10 - api - INFO - Начало загрузки донатов за период: 2025-01-23 14:00:00 - 2025-01-23 15:00:00
2025-01-23 15:00:15 - api - INFO - Загрузка страницы 1...
2025-01-23 15:00:20 - api - INFO - Всего загружено донатов: 5
2025-01-23 15:00:25 - db - INFO - Добавлен новый донат ID 42: @johndoe - продление подписки (сумма: 400)
2025-01-23 15:00:30 - scheduler - INFO - Синхронизация завершена - обработано: 5, добавлено: 3, обновлено: 2, ошибок: 0
```

**Ошибки**:
```
2025-01-23 16:30:00 - api - ERROR - Ошибка API (429): Превышен лимит запросов к API
2025-01-23 16:30:02 - api - WARNING - Rate limit достигнут. Ожидание 2 секунд...
2025-01-23 16:30:05 - api - INFO - Загрузка страницы 1...
```

### Мониторинг логов

**Просмотр в реальном времени**:
```bash
# Все логи
tail -f logs/bot_$(date +%Y-%m-%d).log

# Только ошибки
tail -f logs/errors_$(date +%Y-%m-%d).log

# С фильтрацией
tail -f logs/bot_*.log | grep ERROR
tail -f logs/bot_*.log | grep "johndoe

"
```

**Поиск событий**:
```bash
# Все ошибки за сегодня
grep ERROR logs/bot_$(date +%Y-%m-%d).log

# Действия конкретного пользователя
grep "@johndoe" logs/bot_*.log

# Синхронизации
grep "Синхронизация завершена" logs/bot_*.log

# Создания ссылок
grep "Создана ссылка-приглашение" logs/bot_*.log
```

**Статистика**:
```bash
# Количество запросов приваток
grep "запросил ссылку-приглашение" logs/bot_*.log | wc -l

# Количество синхронизаций
grep "Синхронизация завершена" logs/bot_*.log | wc -l

# Количество ошибок
grep ERROR logs/bot_*.log | wc -l

# Удаленные пользователи
grep "удален из канала" logs/bot_*.log | wc -l
```

### Ротация логов

Рекомендуется регулярно очищать старые логи:

```bash
# Удалить логи старше 30 дней
find logs/ -name "*.log" -mtime +30 -delete

# Архивировать логи старше 7 дней
find logs/ -name "*.log" -mtime +7 -exec gzip {} \;
```

Или настроить автоматическую ротацию через `logrotate`:

```
/path/to/TgBot_manager/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    missingok
}
```

---

## 🔧 Устранение неполадок

### Проблемы при запуске

#### Бот не запускается

**Симптомы**:
```
ERROR - Критическая ошибка при запуске бота
```

**Решения**:

1. Проверьте `.env` файл:
```bash
# Убедитесь, что все обязательные параметры заполнены
cat .env | grep -E "BOT_TOKEN|CHANNEL_ID|ACCESS_TOKEN|ADMIN_IDS"
```

2. Проверьте токен бота:
```bash
# Отправьте запрос к API Telegram
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe"
```

3. Проверьте права на запись в `logs/`:
```bash
mkdir -p logs
chmod 755 logs
```

#### Ошибка "ModuleNotFoundError"

**Решение**:
```bash
pip install -r requirements.txt
```

### Проблемы с донатами

#### Донаты не синхронизируются

**Логи**:
```
ERROR - Ошибка аутентификации (401): неверный или истекший токен
```

**Решения**:

1. Проверьте токен DonationAlerts:
   - Перейдите на https://www.donationalerts.com/application/clients
   - Проверьте срок действия токена
   - При необходимости создайте новый токен

2. Проверьте права токена:
   - Токен должен иметь права на чтение донатов
   - Scope: `oauth-donation-index`

3. Проверьте синхронизацию вручную:
   - Откройте бота в личных сообщениях
   - Отправьте `/sync`

#### Username не извлекается из доната

**Проблема**: Донат есть в DonationAlerts, но не появляется в базе

**Решения**:

1. Проверьте формат сообщения доната:
   - Должен содержать @username или username
   - Примеры правильных форматов:
     - `@johndoe - спасибо!`
     - `johndoe продлеваю подписку`
     - `Донат от @johndoe`

2. Адаптируйте функцию извлечения в `subscription_checker.py`:
```python
async def extract_username_from_message(message_text):
    # Добавьте свои шаблоны поиска
    patterns = [
        r'@(\w+)',  # @username
        r'username[\s:=]+(\w+)',  # username: johndoe
        # Добавьте свой шаблон:
        r'ваш_шаблон'
    ]
```

### Проблемы с каналом

#### "Not enough rights"

**Логи**:
```
ERROR - Недостаточно прав для удаления @username
```

**Решения**:

1. Проверьте, что бот - администратор канала:
   - Откройте канал → Управление каналом → Администраторы
   - Найдите бота в списке

2. Проверьте права бота:
   - ✅ Банить пользователей

3. Проверьте ID канала в `.env`:
```bash
# Для приватного канала должен начинаться с -100
CHANNEL_ID=-1001234567890

# Для публичного канала
CHANNEL_ID=@channel_name
```

#### "User not found"

**Логи**:
```
WARNING - Пользователь @username не найден в канале
```

**Причины**:
- Пользователь уже покинул канал
- Пользователь изменил username
- Username указан неверно в донате

**Решения**:
1. Проверьте, является ли пользователь участником канала
2. Попросите пользователя указать актуальный username в донате
3. Проверьте username в базе данных:
```bash
sqlite3 donations.db "SELECT message FROM donations WHERE message LIKE '%username%';"
```

### Проблемы с доступом

#### Пользователь не может получить ссылку

**Симптомы**:
- Бот пишет "Донаты не найдены"
- Или "Ваша подписка истекла"

**Диагностика**:

1. Проверьте базу данных:
```bash
sqlite3 donations.db "SELECT * FROM donations WHERE message LIKE '%username%';"
```

2. Проверьте синхронизацию:
   - Отправьте админу `/sync`
   - Проверьте логи на наличие ошибок

3. Проверьте username пользователя:
   - Убедитесь, что username указан в донате
   - Проверьте, что username соответствует текущему

#### Ссылка-приглашение не работает

**Причины**:
- Ссылка одноразовая и уже использована
- Ссылка истекла
- Бот не может создавать ссылки (нет прав)

**Решения**:
1. Попросите пользователя запросить новую ссылку
2. Проверьте права бота на создание ссылок-приглашений
3. Проверьте логи:
```bash
grep "Ошибка создания ссылки" logs/bot_*.log
```

### Проблемы с админ-панелью

#### "У вас нет прав администратора"

**Решения**:

1. Проверьте `.env`:
```bash
cat .env | grep ADMIN_IDS
```

2. Получите свой ID:
   - Откройте @userinfobot
   - Отправьте `/start`
   - Добавьте полученный ID в `ADMIN_IDS`

3. Перезапустите бота после изменения `.env`

4. Убедитесь, что ID указаны через запятую без пробелов:
```env
# ✅ Правильно
ADMIN_IDS=123456789,987654321

# ❌ Неправильно
ADMIN_IDS=123456789, 987654321
ADMIN_IDS=123456789 987654321
```

---

## 🔒 Безопасность

### Защита токенов

**КРИТИЧЕСКИ ВАЖНО**:

1. **Никогда** не коммитьте `.env` файл в git:
```bash
echo ".env" >> .gitignore
```

2. Используйте `.env.example` для документации:
```env
# .env.example
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=your_channel_id_here
ACCESS_TOKEN=your_access_token_here
ADMIN_IDS=your_admin_ids_here
```

3. Храните токены в безопасном месте (менеджер паролей)

4. Регулярно меняйте токены при подозрении на компрометацию

### Права бота

**Минимальные необходимые права**:
- ✅ Банить пользователей (для удаления из канала)
- ✅ Создавать ссылки-приглашения (для выдачи доступа)

**НЕ давайте боту**:
- ❌ Права на публикацию сообщений (если не нужно)
- ❌ Права на редактирование информации канала
- ❌ Права на назначение администраторов (если не планируется)

### Валидация данных

Бот автоматически валидирует:

1. **Username**:
   - 5-32 символа
   - Только буквы, цифры, подчеркивание
   - Защита от SQL-инъекций через LIKE escape

2. **Суммы донатов**:
   - Только положительные числа
   - Автоматическая типизация через API

3. **Даты**:
   - ISO 8601 формат
   - Автоматическая валидация при парсинге

### Логирование чувствительных данных

Бот **НЕ** логирует:
- ❌ Полные токены
- ❌ Персональные данные пользователей (только username)
- ❌ Суммы конкретных донатов в открытом виде

Бот логирует:
- ✅ ID пользователей
- ✅ Username (публичная информация)
- ✅ Общую статистику
- ✅ Ошибки (без чувствительных данных)

---

## ❓ FAQ

### Общие вопросы

**Q: Как часто обновляется список донатов?**  
A: Каждый час автоматически. Можно принудительно через `/sync`.

**Q: Почему пользователь не получил доступ после доната?**  
A: Подождите до 1 часа для автоматической синхронизации или запустите `/sync` вручную.

**Q: Можно ли изменить тариф (200₽ = 1 месяц)?**  
A: Да, в файле `db.py`, метод `_calculate_sub_date()`, измените делитель:
```python
months_to_add = int(amount // 200)  # Измените 200 на свое значение
```

**Q: Как добавить нового администратора?**  
A: Добавьте его ID в `.env` файл в параметр `ADMIN_IDS` через запятую, перезапустите бота.

**Q: Можно ли использовать несколько каналов?**  
A: Нет, текущая версия поддерживает только один канал. Для нескольких каналов запустите несколько экземпляров бота.

### Технические вопросы

**Q: Какая база данных используется?**  
A: SQLite3 (файл `donations.db`). Легкая, быстрая, без необходимости установки сервера.

**Q: Можно ли мигрировать на PostgreSQL/MySQL?**  
A: Да, но потребуется переписать класс `DonationDB` в `db.py`.

**Q: Сколько времени хранятся логи?**  
A: Бесконечно. Рекомендуется настроить ротацию (см. раздел "Логирование").

**Q: Можно ли запустить несколько ботов на одном сервере?**  
A: Да, создайте разные директории с разными `.env` файлами и базами данных.

**Q: Как сделать резервную копию данных?**  
A: Скопируйте файл `donations.db`:
```bash
cp donations.db donations_backup_$(date +%Y%m%d).db
```

### Вопросы о донатах

**Q: Что если пользователь указал неправильный username в донате?**  
A: Система не сможет идентифицировать донат. Пользователю нужно сделать новый донат с правильным username или обратиться к администратору для ручного добавления в БД.

**Q: Как обработать донат без username?**  
A: Вручную через SQLite:
```bash
sqlite3 donations.db
INSERT INTO donations (message, amount, last_date, sub) 
VALUES ('@username - донат', 400, '2025-01-23', '2025-03-23');
```

**Q: Донаты накапливаются?**  
A: Да, все донаты суммируются. Например: 200₽ + 400₽ = 600₽ = 3 месяца подписки.

**Q: Можно ли отменить донат?**  
A: Удалите запись из БД:
```bash
sqlite3 donations.db "DELETE FROM donations WHERE message LIKE '%username%';"
```

### Вопросы о безопасности

**Q: Может ли кто-то получить доступ через чужую ссылку?**  
A: Нет, ссылки одноразовые и деактивируются после использования.

**Q: Что если токен бота скомпрометирован?**  
A: Немедленно создайте нового бота через @BotFather и обновите `BOT_TOKEN` в `.env`.

**Q: Видят ли администраторы персональные данные?**  
A: Администраторы видят только username, суммы донатов и даты подписок. Сообщения донатов хранятся в БД, но содержат только username.

---

## 📞 Поддержка

### Сообщение об ошибках

При обнаружении ошибки предоставьте:

1. **Описание проблемы**
2. **Шаги для воспроизведения**
3. **Логи** (из `logs/errors_*.log`)
4. **Версию Python**: `python --version`
5. **Версию aiogram**: `pip show aiogram`

### Контакты

- **Разработчик**: @necoweb
- **Issues**: [Создать issue на GitHub](#)

### Дополнительные ресурсы

- [Документация Aiogram](https://docs.aiogram.dev/)
- [DonationAlerts API](https://www.donationalerts.com/apidoc)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 📝 Changelog

### v1.0.0 (2025-01-23)
- ✅ Начальный релиз
- ✅ Ежечасная синхронизация донатов
- ✅ Ежедневная проверка подписок
- ✅ Админ-панель
- ✅ Автоматическое логирование
- ✅ Валидация username
- ✅ Персональные ссылки-приглашения

---

## 📄 Лицензия

MIT License - свободное использование с указанием авторства.

---

**Автор**: @necoweb  
**Дата**: 23 января 2025  
**Версия документации**: 1.0.0
